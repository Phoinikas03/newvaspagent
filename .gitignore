legacy/
__pycache__/
logs/
runs/
POTCAR/
POTCAR_dir/
workspace/
# LiteLLM 自启的运行期产物：每进程一份 config（含明文上游 API key）与日志。
/.litellm_autostart/
litellm_autostart_config.yaml
litellm_autostart.log
.env
# 仅忽略仓库根的临时运行脚本目录。不加前导斜杠会匹配任意层级的 scripts/，
# 从而把 .claude/skills/*/scripts/ 里的 skill 脚本一并吞掉。
/scripts/
CHGCAR
WAVECAR

# Reproducible / redundant VASP outputs in run dirs (provenance kept in OUTCAR).
runs/**/POTCAR
runs/**/CHG
runs/**/PCDAT
runs/**/IBZKPT
runs/.scheduler/
runs/**/.scheduler/

# 本地跑出来的基准结果与审计输出（rev_*/data、protocol、scripts 是输入与驱动，保留跟踪）
convergence_test/
kspacing_test/
/runs_agent/
rev_*/runs_agent/
rev_*/runs_atomate2/
rev_*/audit/
rev_*/protocol/_dump.txt
/eos_results.json
# 根目录临时输入（模板正本在 .claude/skills/workflow-convergence/templates/）
/INCAR_convergence_template
/POSCAR_mp1265
# 所有 .env 变体（备份也含明文 key），只留示例
.env*
!.env.example
# VASP 赝势受许可证限制，任何位置都不进仓库
POTCAR
POTCAR.spec
