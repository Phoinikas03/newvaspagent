legacy/
__pycache__/
logs/
POTCAR/
workspace/
litellm_autostart_config.yaml
litellm_autostart.log
.env
scripts/
CHGCAR
WAVECAR

# Reproducible / redundant VASP outputs in run dirs (provenance kept in OUTCAR).
# Scoped to runs/ so the POTCAR_dir pseudopotential library stays tracked.
runs/**/POTCAR
runs/**/CHG
runs/**/PCDAT
runs/**/IBZKPT
runs/**/.scheduler/
