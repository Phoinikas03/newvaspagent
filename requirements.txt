# newvaspagent — Python 3.10+
# pip install -r requirements.txt

# Agent 与 Web UI
claude-agent-sdk>=0.1.0
python-dotenv>=1.0.0
aiohttp>=3.9.0

# MCP 工具（src/tool.py）
requests>=2.28.0
urllib3>=2.0.0
markdownify>=0.11.0
duckduckgo-search>=6.0.0
pymatgen>=2024.1.0
mp-api>=0.40.0

# 可选：由 litellm_autostart 通过 subprocess 启动本机 LiteLLM 代理时需单独安装到 PATH
litellm>=1.40.0
