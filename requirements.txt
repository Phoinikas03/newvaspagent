# newvaspagent — Python 3.10+
# pip install -r requirements.txt

# Agent 与 Web UI
claude-agent-sdk>=0.1.0
python-dotenv>=1.0.0
aiohttp>=3.9.0

# MCP 工具（src/tool.py）
requests>=2.28.0
urllib3>=2.0.0
markdownify>=0.11.0
duckduckgo-search>=6.0.0
pymatgen>=2024.1.0
mp-api>=0.40.0

# 可选：src/litellm_proxy 子进程启动本机 LiteLLM 代理
# litellm_autostart.log：缺 websockets/backoff → 需 proxy 额外依赖（见 pip install 'litellm[proxy]'）
litellm[proxy]>=1.40.0
# 使用 SOCKS 代理时拉取 model cost map 等需 socksio（httpx[socks]）
httpx[socks]>=0.27.0

# 可选：scripts/test_xiaoai_openai.py 直连测试 OpenAI 兼容网关
openai>=1.40.0